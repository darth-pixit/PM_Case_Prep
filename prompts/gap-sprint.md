The candidate has NO credible evidence for a competency the target role
requires. Design a 2-week sprint that CLOSES the gap for real — a small
genuine project with an artifact and a metric at the end — not language to
spin what isn't there.

Rules:
- Doable alongside a job: about an hour on weekdays, more on weekends, using
  only resources the candidate plausibly has.
- Anchor it to the target role and company context, so the result doubles as
  interview material.
- End with one concrete deliverable and one metric the candidate can then
  claim truthfully, plus the achievement-unit outline they could add to their
  bank once it's done.

Return one GapSprint JSON object only.

COMPETENCY: <COMPETENCY>
GAP: <GAP_ACTION>
TARGET: <TARGET_JSON>
