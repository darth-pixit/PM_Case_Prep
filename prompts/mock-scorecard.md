Grade the behavioral mock-interview transcript below against the role's
required competencies. For each competency that was actually probed, score
1-4 (1 = red flag, 2 = below bar, 3 = at bar, 4 = exceptional) with one
concrete justification quoting the candidate's own words. Skip competencies
that never came up — an unprobed competency is unknown, not a 1.

Then name the single highest-leverage improvement, and which competencies to
pressure-test next.

Return one MockScorecard JSON object only.

TARGET: <TARGET_JSON>
TRANSCRIPT:
<TRANSCRIPT>
